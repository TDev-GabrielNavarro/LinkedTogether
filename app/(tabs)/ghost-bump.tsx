import { Text, View } from 'react-native';

export default function BumpScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{ fontSize: 24 }}>Bump Screen</Text>
        </View>
    );
}
